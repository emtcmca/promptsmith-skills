node_modules/
.DS_Store

# Output of `npx skills add` run inside this repo (verification installs).
# The shipped skills live in skills/ and are generated by scripts/build-skills.mjs.
.claude/
skills-lock.json
